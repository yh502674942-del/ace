# 核心玩法设计

## 定位

本项目从纯视觉小说扩展为“高中三周恋爱养成 + 分支叙事”。玩家每天放学后有有限行动点，需要在学习、打工、见女主、送礼物之间分配时间。结局不是单纯刷好感，而是由“好感 + 信任 + 核心属性 + 事件触发 + 负罪感”共同决定。

## 循环

1. 阅读剧情。
2. 放学后进入日程 hub。
3. 使用 3 个行动点进行安排。
4. 行动改变金钱、属性、好感、信任，并触发路线事件。
5. 第 14 天后可提前结局判定；第 21 天后自动进入结局。

## 数值

- 金钱：通过便利店打工获得，用于买礼物。
- 行动点：每天 3 点，学习/打工/见面/送礼都消耗 1 点。
- 反馈：行动、买礼物、正确/错误送礼、约会成功、约会未解锁和结束当天都有文案池，按当前日程状态轮换显示。
- 学力：影响共通线和校园自尊主题。
- 共感：白石葵线核心属性。
- 勇气：椎名冬子线核心属性。
- 自省：雾岛遥线核心属性。
- 负罪：过高会把好感转向坏结局，代表“把陪伴误认成拯救”的倾向。
- 回忆：高信任约会后的甜美收藏，不直接替代结局条件，但用于给玩家留下最后会想回看的温暖片段。当前每位女主已有 3 个周末甜美回忆 + 1 个额外回忆；第二、三组与额外回忆配有专属 CG。第三组约会需要第二组约会和对应礼物小任务作为前置，额外回忆会在第三组甜美回忆后自然解锁。
- 通关附录：三条好结局后开放对应后日谈，带独立 CG，可在 Archive 中按角色筛选与重读，用来保留“结局之后仍然发亮”的甜美余味。
- 库存：礼物需要先购买再送出；送错礼物会轻微增加好感，但降低信任并提高负罪感。当前礼物分为基础礼物与进阶礼物，进阶礼物需要对应角色达到一定好感/信任后才可购买，并会触发带 CG 的礼物小任务。

## 角色路线

- 雾岛遥：图书馆线。核心是克制、被看见、不要把自己误认成太阳。
- 白石葵：医务室线。核心是告别、边界、不要逼问脆弱。
- 椎名冬子：学生会/便利店线。核心是尊严、帮助的尺度、接受也是尊重。

## 表现层

- 角色节点应优先指定或自动推断 `portrait`，用微表情补足对白的情绪。
- 每个女主至少维护 5 类表情：默认克制、甜味/放松、害羞/动摇、不安/忍泪、别扭/道歉。
- 服装随场景变化：图书馆/教室用校服，旧书店/河边可用针织衫或开衫，雨天用外套，车站用便服或围巾，便利店相关场景用围裙；高信任约会可使用更有“约会感”的克制差分，如文化祭私服、修学旅行睡衣开衫、泳池罩衫。高中线可以有轻微心动和福利感，但必须保持得体、完整穿着、自然姿态，不做性化镜头，不把服装当作凝视奖励。
- 甜剧情要保留轻微的克制：不做夸张糖分，而是让玩家记得伞下半步、纸船、热咖啡这类小动作。
- Archive 包含角色档案、甜美回忆、事件回想、通关附录、通关后收集提示、回看札记和结局收集。甜美回忆与通关附录可展示已解锁 CG；事件回想会为已解锁节点显示缩略图，礼物小任务优先使用专属 CG；事件和附录回想都能重读已触发节点，并能返回当前进度；通关后收集提示会按已解锁的结局、后日谈、回忆和回收节点显示完成度，完成后提供关键片段重读入口；回看札记会在关键分歧或普通结局余韵完成后显示主题读法与重读入口；Archive 可按角色筛选。
- v12 起，三条路线的第六事件有专属 CG，并在节点结束时出现中段分歧。克制回应会提高信任、降低负罪；冒进回应会提高少量好感但降低信任、增加负罪，并写入 `badForeshadow*` 标记，为后续坏结局预兆和普通结局回收提供钩子。
- v13 起，结局判定会在进入最终结局前读取 `badForeshadow*`。如果玩家后来补回信任并抵达好结局，会先进入 `prelude_*_repair`；如果进入坏结局，会先进入 `prelude_*_bad`；如果落入普通结局，会先进入 `prelude_*_normal`。这些节点不改变数值，只回收中段分歧的叙事后果。
- v14 起，三条路线的第 4 事件有专属 CG，并同步作为 Archive 事件回想缩略图：遥的闭馆校门、葵的转学通知、冬子的夜班收银。
- v15 起，三条路线的第 5 事件也有专属 CG：遥的雨窗“不是太阳”、葵的未送纸鹤、冬子的值日表背面。至此第 4-6 个核心路线事件均有 Archive 缩略图。
- v16 起，错误礼物会写入 `wrongGiftHaruka`、`wrongGiftAoi`、`wrongGiftFuyuko` 标记。结局判定若没有更高优先级的中段分歧回收，会按好结局修复、坏结局、普通结局进入 9 个错误礼物回收节点，再回到对应结局。
- v17 起，三张新增表情差分接入关键情绪：遥“被误解后的冷淡”、葵“甜味后的心虚”、冬子“重读回忆时的释然”。结局判定在没有中段分歧与错误礼物回收时，会根据高好感低信任状态进入 6 个低信任独白节点，再回到坏结局或普通结局。
- v18 起，结局判定会在低信任回收前检查核心路线事件数。好感已达标但事件数不足时，会进入 6 个错过核心事件/低事件数独白节点，再回到坏结局或普通结局。
- v19 起，三条好结局后日谈接入新的表情差分：遥“重逢前的犹豫”、葵“害羞后装作镇定”、冬子“轻松普通笑”。这些差分只更新通关附录表现，不改变结局或数值。
- v20 起，结局判定会读取本路线已解锁甜美/额外回忆数量。回忆太少时，会进入 6 个错过约会独白节点。
- v21 起，错过约会 CG 已全部替换为女主入镜版本；普通结局会在没有更具体回收时进入对应女主的余韵节点，并使用三张普通结局余韵 CG。通用普通结局 CG 也改为三位女主同屏。剧情、回收和结局 CG 不再接入只有背景没有女主的画面，新增图必须让对应女主自然入镜。
- v22 起，Archive 新增通关后收集提示。提示只读取既有解锁状态与现有人物 CG，不改变 AP、金钱、结局阈值、路线事件数或任何数值收益。
- v23 起，Archive 新增回看札记。札记只读取既有关键分歧、普通结局余韵和结局解锁状态，完成后显示既有人物 CG 与主题读法，不新增空景资源，也不改变任何路线判定。
- v24 起，遥线新增“认真道歉后的放松”表情差分，并只接入中段分歧修复与错误礼物修复回收节点，用来区分“松了一口气”与“认真承认错误”的细微情绪。
- v25 起，Archive 新增三条低信任回看札记。它们只读取对应女主低信任普通回收节点是否已解锁，完成后使用既有女主入镜普通余韵 CG 和重读入口，不新增空景资源，也不改变任何路线判定。
- v26 起，Archive 新增三条普通余韵角色札记。它们要求普通结局和对应女主普通余韵同时解锁，完成后复用既有女主入镜普通余韵 CG，让玩家按角色重读“普通结局并非空白”的主题。
- v27 起，Archive 新增三条错误礼物回看札记。它们要求对应女主的错误礼物普通回收节点解锁，完成后复用既有女主入镜普通余韵 CG，让送错礼物的粗心也能被回看成主题的一部分。
- v28 起，Archive 新增三条坏结局预兆回看札记。它们要求对应女主的冒进分歧节点解锁，完成后复用既有女主入镜路线事件 CG，让“关心第一次太亮”的瞬间也能被玩家回看。
- v29 起，Archive 新增三条好结局后日谈回看札记。它们要求对应女主好结局和通关附录同时解锁，完成后复用既有女主入镜后日谈 CG，让坏句子、空白纸鹤和第二罐咖啡成为通关后可反复阅读的主题锚点。
- v30 起，遥线好结局接入“告别前普通笑”表情差分。它使用完整校服针织衫与书本动作，让毕业前的轻笑区别于后日谈里的重逢犹豫，也不改变任何数值或结局条件。
- v31 起，葵线修复回收接入“低信任修复后的安心”表情差分。它使用完整校服外套、医务室窗帘动作和克制微笑，强调安心来自允许她把窗只开一条缝，不改变任何数值或结局条件。
- v32 起，普通结局三条女主余韵补入更明确的克制笑落点，并新增三条 Archive 普通余韵克制笑札记。该版本不新增图片，全部复用既有女主入镜普通余韵 CG，只增加文本读法和重读入口。
- v33 起，葵线与冬子线好结局补入告别前普通笑的正文落点，并新增三条 Archive 好结局告别微表情札记。该版本不新增图片，全部复用既有好结局 CG，只增加文本读法和重读入口。
- v34 起，葵线好结局接入“告别前普通笑”表情差分。它使用完整校服外套、课本和纸鹤动作，让“黄昏不再道歉”的短促轻笑从文字落点变成可见状态，不改变任何数值或结局条件。
- v35 起，Archive 角色档案新增三条“边界读法”，把每条路线通关后的主题压缩成可反复查看的档案文字；冬子好结局补入“没有急着还清”的动作落点，强调接受温柔也是尊重。
- v36 起，冬子线好结局接入“告别前普通笑”表情差分。它使用完整学生会制服、值日表和罐装咖啡动作，让“接受也是尊重”的克制笑从文字落点变成可见状态，不改变任何数值或结局条件。
- v37 起，遥线普通余韵接入“普通结局里的克制笑”表情差分。它使用完整冬季校服、旧书和黄昏教室动作，让“合上书前的淡笑”从回看札记读法变成可见状态，不改变任何数值或结局条件。
- v38 起，葵线普通余韵接入“普通结局里的克制笑”表情差分。它使用完整校服外套、纸鹤和医务室窗帘动作，让“折痕上留下的小笑”从回看札记读法变成可见状态，不改变任何数值或结局条件。
- v39 起，冬子线普通余韵接入“普通结局里的克制笑”表情差分。它使用完整学生会制服、值日夹、便利店后门和两罐咖啡动作，让“端正得几乎看不出来的笑”从回看札记读法变成可见状态，不改变任何数值或结局条件。
- v40 起，冬子线修复回收接入“认真道歉后的放松”表情差分。它使用完整学生会制服、毕业日教室、值日夹和值日表动作，让“终于没有被硬推开”的释然从夜晚便服重读表情中独立出来，不改变任何数值或结局条件。
- v41 起，葵线错误礼物修复回收接入“错误礼物修复后的放松”表情差分。它使用完整校服外套、医务室黄昏、纸袋和纸鹤动作，让“你记得这个啊”之后的轻微释然区别于低信任修复的安心，不改变任何数值或结局条件。
- v42 起，遥线错误礼物修复回收接入“错误礼物修复后的放松”表情差分。它使用完整冬季校服针织衫、闭馆前图书馆、小票和旧书动作，让“小票背面的道歉”从通用认真道歉状态中独立出来，不改变任何数值或结局条件。
- v43 起，三位女主的默认基础立绘改为差异化版本：遥为雨窗图书馆长发旧书、葵为医务室短发纸鹤开衫、冬子为学生会教室低马尾值日表与咖啡。遥线核心事件走完后的图书馆行动也新增 5 段轮换日常文本，减少后期重复感；该改动不改变 AP、金钱、好感、信任、负罪或结局阈值。
- v46 起，Archive 重读入口会保存进入前快照，并在重读结束时恢复原位置、原状态和标题页可见性。该改动只影响回看体验，不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v47 起，葵线坏预兆修复回收接入“承诺变轻后的放松”表情差分。它使用完整校服开衫、医务室黄昏、纸鹤放进口袋的动作，让“这样就够了”从低信任修复状态里独立出来，不改变任何数值或结局条件。
- v48 起，冬子线便利店后门接入“白灯下的克制放松”半身差分。它使用完整制服外套、便利店围裙、咖啡罐和后门白灯，让“只是坐在这里”的尊重从通用围裙暖笑里独立出来，不改变任何数值或结局条件。
- v49 起，AP 用尽后的日程页不再继续渲染行动、约会、购买和送礼按钮，只保留推进到下一天或结局判定的按钮。该改动只改善“卡住感”和选择区可读性，不改变 AP、金钱、好感、信任、负罪、路线事件或结局阈值。
- v50 起，三条普通结局角色余韵会先汇合到“三个人各自亮过的黄昏”公共余韵节点，再进入通用普通结局。该节点使用三位女主同屏毕业走廊 CG，让普通结局的“没有抵达，也曾照见”有更明确的画面落点，不改变任何数值或结局条件。
- v51 起，葵线与冬子线核心事件完成后的日程行动也改为 5 段轮换文本，补齐遥线 v43 后期日常的体验密度；Archive 未完成条件会显示“路线事件/分歧事件/甜美回忆/结局”等类型和还差项，降低“玩过但没解锁”的误解。该改动不改变 AP、金钱、好感、信任、负罪、路线事件计数或结局阈值。
- v52 起，冬子线错误礼物修复回收接入“错误礼物修复后的克制释然”表情差分。它使用完整学生会制服、毕业日教室、纸袋和咖啡罐动作，让“收下也不是欠下”的释然从通用认真道歉状态中独立出来，不改变任何数值或结局条件。
- v53 起，遥线家庭通知单正向分歧接入“通知单旁被沉默接住后的释然”表情差分。它使用完整冬季校服、雨窗图书馆、皱掉通知单和钢笔动作，让“不追问”的信任从通用道歉后放松状态中独立出来，不改变任何数值或结局条件。
- v54 起，葵线体温计正向分歧接入“开窗后被允许不命名痛苦的安心”表情差分。它使用完整校服开衫、医务室白窗帘、体温计和纸鹤动作，让“只问要不要开窗”的克制从通用惊讶状态中独立出来，不改变任何数值或结局条件。
- v55 起，基础养成行动也支持轮换日常。晚自习和便利店打工各新增 5 段无数值节点，`fallbackTargets` 不再只服务角色行动，用来减少玩家后期刷属性和金钱时的重复感；该改动不改变 AP、金钱、好感、信任、负罪、路线事件计数、Archive 解锁条件或结局阈值。
- v56 起，Continue/Load 会统一执行 Archive 解锁修复与持久化，日程页底部提示会按节点类型切换，避免 hub 节点仍提示“点击继续”造成卡住误解；该改动不改变任何剧情、数值或结局条件。
- v57 起，Archive 新增三条普通结局公共余韵回看札记，要求普通结局与三人走廊公共余韵已解锁，复用 `normalEndingAfterglowTrio`，补足“名字不是选项 / 没有告白 / 回头不是重来”的读法；该改动不改变 AP、金钱、好感、信任、负罪、路线事件计数、既有 Archive 条目的解锁条件或结局阈值。
- v58 起，送错礼物不再只进入通用 `gift_wrong`，而会按目标女主进入 `gift_wrong_haruka`、`gift_wrong_aoi`、`gift_wrong_fuyuko`；Archive 新增三条错误礼物即时回看札记，记录当场误解和礼貌背后的距离。该改动不改变错误礼物 AP 消耗、库存扣除、好感、信任、负罪、wrongGift flags、结局优先级或阈值。
- v59 起，遥线基础正确礼物“旧文学杂志”接入“正确礼物后的页边安心”表情差分，并新增一条正确礼物回看札记。该改动只让“送对礼物为什么对”有可见读法，不改变礼物成本、效果、AP、库存、结局优先级或阈值。
- v60 起，葵线基础正确礼物“纸鹤信笺”接入“正确礼物后的纸鹤安心”表情差分，并新增一条正确礼物回看札记。该改动只让“送对礼物为什么对”有可见读法，不改变礼物成本、效果、AP、库存、结局优先级或阈值。
- v61 起，冬子线基础正确礼物“罐装咖啡”接入“正确礼物后的咖啡释然”表情差分，并新增一条正确礼物回看札记。该改动补齐三位女主基础正确礼物的可见读法，不改变礼物成本、效果、AP、库存、结局优先级或阈值。
- v62 起，遥线进阶礼物“留白便签”新增一条进阶礼物回看札记，复用已有礼物小任务 CG。该改动只让进阶礼物任务的主题可回看，不改变购买门槛、礼物效果、AP、库存、结局优先级或阈值。
- v63 起，葵线进阶礼物“窗边创可贴盒”和冬子线进阶礼物“透明文件袋”各新增一条进阶礼物回看札记，复用已有礼物小任务 CG。该改动补齐三位女主进阶礼物任务后的主题读法，不改变购买门槛、礼物效果、AP、库存、结局优先级或阈值。
- v64 起，葵线进阶礼物“窗边创可贴盒”接入“创可贴盒旁被允许慢慢好的释然”表情差分，并扩写任务正文中按住盒盖、肩膀放松的动作落点。该改动只替换礼物小任务的可见表情，不改变购买门槛、礼物效果、AP、库存、结局优先级或阈值。
- v65 起，遥线进阶礼物“留白便签”接入“留白便签旁被允许空着的释然”表情差分，并扩写任务正文中便签翘起一角、给风和犹豫留位置的动作落点。该改动只替换礼物小任务的可见表情，不改变购买门槛、礼物效果、AP、库存、结局优先级或阈值。
- v66 起，冬子线进阶礼物“透明文件袋”接入“透明文件袋旁把秩序交还自己的释然”表情差分，并扩写任务正文中推好拉链、按平袋口折痕的动作落点。该改动只替换礼物小任务的可见表情，不改变购买门槛、礼物效果、AP、库存、结局优先级或阈值。

## 结局规则

- 好结局：最高角色综合分 >= 38，且好感 >= 10、信任 >= 7、核心事件触发 >= 5、核心属性 >= 6、负罪 <= 7。
- 坏结局：好感 >= 6，但信任 < 5、核心事件触发 < 4，或负罪 >= 8。
- 普通结局：未满足以上条件。

## v43 路线平衡

- 已用脚本模拟三条路线的 21 天通关：每条路线都能在不送错礼物的前提下完成 6 个核心事件、3 件路线礼物、3 个周末约会、正确中段分歧，并在第 21 天抵达对应好结局。
- 已单独验证冒进分歧会按结局类型进入修复、坏结局或普通结局回收节点，不影响正常好结局路线。
- v14-v15 补图只更新背景和回想缩略图，不改变 AP、金钱、数值阈值或结局判定。
- v16 错误礼物回收只新增 flags 与结局前节点，不改变错误礼物原有好感、信任、负罪影响，也不改变好/坏/普通结局阈值。中段分歧 `badForeshadow*` 的回收优先级高于错误礼物回收。
- v17 表情差分只改变节点 portrait；低信任回收只在 `badForeshadow*` 与 `wrongGift*` 都没有命中时触发，不改变结局阈值和数值收益。坏结局低信任阈值为好感 >= 10 且信任 < 5；普通结局低信任阈值为好感 >= 10 且信任 < 7。
- v18 错过事件回收只读取既有 `events` 数值，不增加或扣减 AP、金钱、好感、信任、属性或负罪。触发优先级为：中段分歧、错误礼物、错过核心事件/低事件数、低信任。坏结局事件阈值为好感 >= 6 且事件数 < 4；普通结局事件阈值为好感 >= 10 且事件数 < 5。
- v19 后日谈表情差分只改变 `epilogue_*_good` 的 portrait，不影响 AP、金钱、事件数、结局判定或 Archive 解锁条件。
- v20 错过约会回收只读取 `state.memories` 中属于当前女主的回忆数，不改变 AP、金钱、好感、信任、事件数、属性或负罪。触发优先级追加在低信任之后；好结局前若本路线回忆少于 2 个进入修复型错过约会独白，普通结局前若本路线回忆为 0 且事件数 >= 5 进入普通型错过约会独白。
- v21 普通结局余韵只在普通结局且没有更高优先级回收时触发；错过约会、错误礼物、低信任和错过事件的普通结局节点会先播放自身独白，再串到对应女主的余韵节点，最后进入普通结局。该改动不改变结局阈值、数值收益或 Archive 解锁规则。
- v21 图片调整只替换资源引用：`missed-date-*-hero.png`、`normal-aftertaste-*.png` 和 `ending-normal-heroines.png` 都是带女主入镜的 CG，不改变 AP、金钱、事件数、结局判定或文本解锁条件。
- v22 Archive 收集提示只读取 `endingDetails`、`epilogueDetails`、`memories` 与 `scenes` 的解锁状态；未完成时只显示文字进度，完成后才显示既有人物 CG 与重读按钮，不新增空景资源，也不参与结局判定。
- v23 Archive 回看札记只读取 `scenes` 与 `endingDetails` 的解锁状态；未完成时只显示提示，完成后才显示既有人物 CG 与重读按钮，不新增 AP 消耗、数值奖励、结局阈值或路线事件。
- v24 遥线表情差分只新增 `portrait-haruka-apology-relaxed.png` 和两个节点的 `portrait` 指向，不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v25 低信任回看札记只新增 `gameplay.reviewNotes` 条目，不增加 AP 消耗、数值奖励、结局阈值、路线事件或新图片；锁定时不露图，完成后复用现有女主入镜普通余韵 CG。
- v26 普通余韵角色札记只新增 `gameplay.reviewNotes` 条目，不增加 AP 消耗、数值奖励、结局阈值、路线事件或新图片；锁定时不露图，完成后复用现有女主入镜普通余韵 CG，并提供对应余韵节点的重读入口。
- v27 错误礼物回看札记只新增 `gameplay.reviewNotes` 条目，不增加 AP 消耗、数值奖励、结局阈值、路线事件或新图片；锁定时不露图，完成后复用现有女主入镜普通余韵 CG，并提供对应错误礼物普通回收节点的重读入口。
- v28 坏结局预兆回看札记只新增 `gameplay.reviewNotes` 条目，不增加 AP 消耗、数值奖励、结局阈值、路线事件或新图片；锁定时不露图，完成后复用现有女主入镜第 6 事件 CG，并提供对应冒进分歧节点的重读入口。
- v29 好结局后日谈回看札记只新增 `gameplay.reviewNotes` 条目，不增加 AP 消耗、数值奖励、结局阈值、路线事件或新图片；锁定时不露图，完成后复用现有女主入镜后日谈 CG，并提供对应通关附录的重读入口。
- v30 遥“告别前普通笑”只新增 `portrait-haruka-goodbye-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `ending_haruka_good` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v31 葵“低信任修复后的安心”只新增 `portrait-aoi-lowtrust-relief.png`、一个 `portraits` / `portraitStates` 条目；当时用于 `prelude_aoi_repair` 的修复状态，v47 后该节点改用更贴合“承诺变轻”的独立差分。不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v32 普通余韵克制笑只新增三条 `gameplay.reviewNotes` 条目，并扩写三个 `prelude_*_normal_aftertaste` 文本；不新增图片、不增加 AP 消耗、数值奖励、路线事件或结局阈值，锁定时不露图，完成后复用现有女主入镜普通余韵 CG。
- v33 好结局告别微表情只新增三条 `gameplay.reviewNotes` 条目，并扩写 `ending_aoi_good` 与 `ending_fuyuko_good` 文本；不新增图片、不增加 AP 消耗、数值奖励、路线事件或结局阈值，锁定时不露图，完成后复用现有好结局 CG。
- v34 葵“告别前普通笑”只新增 `portrait-aoi-goodbye-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `ending_aoi_good` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v35 档案边界读法只新增三个 `heroines.*.boundaryNote` 字段，并在 Archive 角色档案中展示；冬子好结局只扩写正文，不新增 AP 消耗、数值奖励、路线事件、图片资源或结局阈值。
- v36 冬子“告别前普通笑”只新增 `portrait-fuyuko-goodbye-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `ending_fuyuko_good` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v37 遥“普通结局里的克制笑”只新增 `portrait-haruka-normal-restraint-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_haruka_normal_aftertaste` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v38 葵“普通结局里的克制笑”只新增 `portrait-aoi-normal-restraint-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_aoi_normal_aftertaste` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v39 冬子“普通结局里的克制笑”只新增 `portrait-fuyuko-normal-restraint-smile.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_fuyuko_normal_aftertaste` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v40 冬子“认真道歉后的放松”只新增 `portrait-fuyuko-apology-relaxed.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_fuyuko_repair` 与 `prelude_fuyuko_wrong_gift_repair` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v41 葵“错误礼物修复后的放松”只新增 `portrait-aoi-wronggift-relaxed.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_aoi_wrong_gift_repair` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v42 遥“错误礼物修复后的放松”只新增 `portrait-haruka-wronggift-relaxed.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_haruka_wrong_gift_repair` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v43 三位差异化基础立绘只替换 `harukaQuiet`、`aoiTender`、`fuyukoComposed` 的资源路径；遥线后期图书馆日常只新增 fallbackTargets 与 5 个无数值节点，不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v46 Archive 重读快照只新增运行时返回状态，不新增资源、节点、数值奖励、AP 消耗、路线事件或结局阈值。
- v47 葵“承诺变轻后的放松”只新增 `portrait-aoi-promise-relaxed.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_aoi_repair` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v48 冬子“后门白灯下的克制放松”只新增 `portrait-fuyuko-backdoor-composed.png`、一个 `portraits` / `portraitStates` 条目，并将 `fuyuko_006` 与 `branch_fuyuko_backdoor_sit` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v49 AP=0 日程页只调整 `renderHub()` 的按钮渲染顺序与范围，不新增节点、资源、AP 消耗、数值奖励、路线事件或结局阈值。
- v50 普通结局公共余韵只新增 `normal-ending-afterglow-trio.png`、一个 `backgrounds` 条目、一个 `gameplay.scenes` 条目和 `prelude_normal_afterglow_trio` 节点，并将三条 `prelude_*_normal_aftertaste` 的 `next` 指向该节点；不新增 AP 消耗、数值奖励、路线事件或结局阈值。
- v51 后期日常扩展只为葵线与冬子线新增各 5 个无数值 fallback 节点，并将对应行动加入 `fallbackTargets`；Archive 提示改动只调整显示文案和打开时的当前进度修复，不新增资源、AP 消耗、数值奖励、路线事件计数或结局阈值。
- v52 冬子“错误礼物修复后的克制释然”只新增 `portrait-fuyuko-wronggift-relaxed.png`、一个 `portraits` / `portraitStates` 条目，并将 `prelude_fuyuko_wrong_gift_repair` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v53 遥“通知单旁被沉默接住后的释然”只新增 `portrait-haruka-notice-silence-relief.png`、一个 `portraits` / `portraitStates` 条目，并将 `branch_haruka_notice_silence` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v54 葵“开窗后被允许不命名痛苦的安心”只新增 `portrait-aoi-window-relief.png`、一个 `portraits` / `portraitStates` 条目，并将 `branch_aoi_thermometer_window` 的 `portrait` 指向该差分；不改变 AP、金钱、好感、信任、负罪、事件数、Archive 解锁条件或结局阈值。
- v55 基础行动轮换只为晚自习与便利店打工新增各 5 个无数值 fallback 节点，并让非角色行动也可使用 `fallbackTargets`；不新增资源、AP 消耗、数值奖励、路线事件计数、Archive 解锁条件或结局阈值。
- v56 Archive/提示自查只调整运行时同步和底部提示文案，不新增节点、资源、AP 消耗、数值奖励、路线事件计数或结局阈值。
- v57 普通结局公共余韵札记只新增三条 `gameplay.reviewNotes` 条目，复用 `normalEndingAfterglowTrio`，不新增节点、资源、AP 消耗、数值奖励、路线事件计数或结局阈值。
- v58 错误礼物即时反应只新增 3 个无数值剧情节点、3 个 `gameplay.scenes` 条目和 3 条 `gameplay.reviewNotes`；`performGift()` 分流不改变错误礼物原有效果、AP 消耗、库存扣除、结局优先级或阈值。
- v59 遥正确礼物细读法只新增 `portrait-haruka-correctgift-margin-relief.png`、一个 `backgrounds` / `portraits` / `portraitStates` 条目、一个 `gameplay.scenes.gift_haruka` 条目和一条 `gameplay.reviewNotes`；`gift_haruka` 原有礼物效果、AP 消耗、库存扣除和结局阈值不变。
- v60 葵正确礼物细读法只新增 `portrait-aoi-correctgift-crane-relief.png`、一个 `backgrounds` / `portraits` / `portraitStates` 条目、一个 `gameplay.scenes.gift_aoi` 条目和一条 `gameplay.reviewNotes`；`gift_aoi` 原有礼物效果、AP 消耗、库存扣除和结局阈值不变。
- v61 冬子正确礼物细读法只新增 `portrait-fuyuko-correctgift-coffee-relief.png`、一个 `backgrounds` / `portraits` / `portraitStates` 条目、一个 `gameplay.scenes.gift_fuyuko` 条目和一条 `gameplay.reviewNotes`；`gift_fuyuko` 原有礼物效果、AP 消耗、库存扣除和结局阈值不变。
- v62 遥进阶礼物细读法只新增一条 `gameplay.reviewNotes.harukaAdvancedGiftMargin`，复用 `taskHarukaInkMargin` 与 `task_haruka_margin_note`；`marginSticky` 原有购买门槛、礼物效果、AP 消耗、库存扣除、任务 flag 和结局阈值不变。
- v63 葵/冬子进阶礼物细读法只新增 `gameplay.reviewNotes.aoiAdvancedGiftBandage` 与 `gameplay.reviewNotes.fuyukoAdvancedGiftFileBag`，分别复用 `taskAoiCraneMilktea` / `taskFuyukoDutyCoffee` 与对应任务节点；`windowBandage`、`clearFileBag` 原有购买门槛、礼物效果、AP 消耗、库存扣除、任务 flag 和结局阈值不变。
- v64 葵进阶礼物表情差分只新增 `portrait-aoi-advancedgift-bandage-relief.png`、一个 `portraits` / `portraitStates` 条目，并将 `task_aoi_window_bandage` 的 `portrait` 指向该差分；`windowBandage` 原有购买门槛、礼物效果、AP 消耗、库存扣除、任务 flag 和结局阈值不变。
- v65 遥进阶礼物表情差分只新增 `portrait-haruka-advancedgift-margin-relief.png`、一个 `portraits` / `portraitStates` 条目，并将 `task_haruka_margin_note` 的 `portrait` 指向该差分；`marginSticky` 原有购买门槛、礼物效果、AP 消耗、库存扣除、任务 flag 和结局阈值不变。
- v66 冬子进阶礼物表情差分只新增 `portrait-fuyuko-advancedgift-filebag-relief.png`、一个 `portraits` / `portraitStates` 条目，并将 `task_fuyuko_file_bag` 的 `portrait` 指向该差分；`clearFileBag` 原有购买门槛、礼物效果、AP 消耗、库存扣除、任务 flag 和结局阈值不变。
- 关键节奏：第 1-3 天可完成主要礼物/路线事件积累；第 6、7、13 天可依次完成三组周末约会；第 14 天以后保留大量 AP 给学习、打工和降负罪，不会被最终阈值卡死。
- 雾岛遥线需要为进阶礼物额外打工一次；白石葵线和椎名冬子线初始金钱即可覆盖三件路线礼物。所有路线在规范游玩下负罪可控制在 7 以下。
- 送礼和约会不计入核心事件数，因此玩家若想拿好结局，必须至少主动进入对应女主路线 5 次；这是有意设计，用来避免只靠金钱和礼物跳过“看见对方”的过程。

## 后续自动化补写目标

- 继续增加表情差分：普通结局后半段更多轻微释然状态，或补充更多路线分歧后的克制放松状态。
- 第四、第五、第六路线事件已有专属 CG；错误礼物、低信任、错过核心事件/低事件数、错过约会、普通结局角色余韵和普通结局公共走廊余韵已有结局前独白回收；Archive 通关后收集提示、关键回看札记、低信任回看札记、普通余韵角色札记、普通余韵克制笑札记、普通结局公共余韵札记、好结局告别微表情札记、错误礼物回看札记、错误礼物即时札记、正确礼物回看札记、坏结局预兆札记和好结局后日谈札记已接入。下一步继续补冬子的正确礼物细读法或更多轻微释然状态。
- 结局 CG 已有好/坏各 3 张、通用普通结局 1 张、普通结局公共走廊余韵 1 张；通关附录已有 3 张后日谈 CG；礼物小任务已有 3 张专属 CG；第三组甜美回忆已有 3 张专属 CG；额外回忆已有雨窗书签、天桥风铃、雨夜巴士站 3 张专属 CG；普通结局角色余韵已有 3 张女主入镜 CG。下一步可补更多事件回想图标或小缩略图。
- 下一轮优先补冬子的正确礼物细读法、购买礼物后的轻反馈，或更多轻微释然状态；如果继续扩展日程，可考虑给购买/送礼后的特殊失败与修复增加更多读法。新增 CG 要避免纯空背景，确保女主自然出现在画面中；继续留意 Archive 解锁说明是否让玩家误会“玩过某路线”等于“完成对应札记全部条件”。
